# Состояние — 23.09.2026

## Обновление: агентная ветка

Основной MVP опубликован в `main`. В отдельной `agent-integration` добавлены
`replenishment/agent.py`, `agent_client.py`, опциональный `requirements-agent.txt`,
безопасные пустые переменные в `.env.example`, тесты и `AGENT_CONTRACT.md` для DEV B.
Объединение с `main` не выполнялось. `app.py`, движок, модели и review не изменены.

Фактические проверки этой ветки:

- До правок: **49 passed in 4.87s**.
- `python -m pytest -v`: **90 passed in 5.07s**; существующие тесты не удалялись.
- `python -m pip check`: **No broken requirements found**.
- Учебный CLI: 4 позиции `ok`, по 85 единиц; всплеск 5000 исключён у SPIKE.
- Реальный OpenAI SDK 3.19.0 с подменённым HTTP transport: 3 запроса, tools
  `calculate_replenishment` → `prepare_order`, результат `pending_approval`, 85 единиц.
  Сеть и реальные credentials не использовались; это НЕ проверка живой модели.

Без ключа доступен локальный расчёт. Ошибки модели не создают заказ. Тесты проверяют
блокировку неподдерживаемых tools, checkbox, неполные данные, отсутствие экспорта до
Approve и инвалидацию изменённого проекта. Сырые исключения не логируются.
Свободный текст модели остаётся недоверенным; живое качество и устойчивость к prompt
injection не доказаны. Real-data импорт повторно в этой ветке не запускался.
Чистая установка на втором компьютере и новый агентный UI-сценарий ещё не проверены.

Дальше: согласовать объединение ветки, подключить контракт в UI у DEV B, настроить
выбранную модель локально и проверить live-сценарий на синтетических данных.

## Исходный снимок MVP (до публикации)

Локальный рабочий MVP реализован в репозитории организатора. Коммит и push в рамках
этого шага не выполнялись. Исходные частные Excel остались вне репозитория.

## Создано

- Архитектура, контракт DEV A/DEV B и отдельное задание напарнику.
- Два загрузчика выданных форматов: продажи, накладные, история остатков,
  поставки, MOQ/кратность и сезонность.
- Pydantic-модели и расчёт с источниками, предупреждениями и явными статусами.
- Отдельный подписанный учебный набор и автономный CLI.
- Streamlit: загрузка, параметры, объяснение, ручные уточнения, корректировка,
  подтверждение/отклонение конкретной версии, CSV.
- requirements.txt с проверенными прямыми зависимостями, безопасный .env.example,
  README и .gitignore.

## Фактически выполненные проверки

Окружение: Windows, Python 3.14.7, проектная `.venv`.

`python -m pytest -v`: **49 passed in 3.66s**, без предупреждений.
`python -m pip check`: **No broken requirements found**.
`python -m compileall -q app.py replenishment tests`: код завершения 0.

Учебный CLI: `python -m replenishment --demo --as-of 2026-09-22 --horizon 30 --lead 7`.
Четыре строки `ok`. В каждой паре REGULAR/SPIKE рекомендация **85 штук**;
у SPIKE исключено **5000**, у REGULAR — 0. Это синтетическая проверка, не результат компании.

Реальный CLI: оба предоставленных каталога, дата **22.09.2026**, сценарные параметры
**30 дней между закупками + 7 дней поставки**. Параметры не объявлены условиями компании.

- Все 12 источников прочитаны со статусом `ok`.
- Объединение: **3909 ключей поставщик + код 1С**.
- IEK: **3145 needs_data**, **40 invalid_data**, числовые заказы не сфабрикованы.
- Systeme Electric: **378 ok**, **314 needs_data**, **32 invalid_data**.
- Среди рассчитанных позиций **23** с положительным рекомендуемым количеством.
- Прочитано 171579 накладных IEK и 77296 Systeme Electric; прочие документы
  не прибавлялись к месячным продажам.

Streamlit AppTest с настоящими двумя каталогами: загрузка и расчёт — **0 исключений**;
метрики 378 рассчитано, 3531 требуют проверки данных, 23 к закупке; approval = None.
Последующая точечная поправка роста проверена тестами и повторным реальным CLI:
исходный коэффициент не возвращает исключённый выброс обратно в прогноз.

Локальный сервер запущен на **http://127.0.0.1:8502**; endpoint `_stcore/health` вернул `ok`.
Ручная визуальная проверка в браузере человеком не выполнялась.

## Ограничения, которые нельзя называть закрытыми

- Нет клиентских ID в реальных детализациях: тест уровня клиента синтетический.
- Нет точных длительностей stockout: исходные нули дают предупреждение, а не
  вымышленные дни. Численная компенсация требует подтверждённого ввода.
- Нет актуального свободного остатка IEK; пустые количества в поставках не приравнены
  к нулю. Этим объясняется большая доля заблокированных реальных строк.
- Нет расшифровки политики категорий и сроков поставщика. Категории — исходные коды,
  срок и горизонт — явные параметры. Backend допускает заданный менеджером горизонт
  по категории, UI предоставляет отбор по категориям.
- CSV не проверен на импорт в конкретную 1С. Экономический эффект и точность реального
  прогноза не оценивались. Полное прохождение пяти Must have не заявляется.

## Следующий шаг команды

Просмотреть работающий UI и ограничения, передать напарнику `DEV_B_TASK.md` и
`CONTRACT.md`, согласовать владельца app.py. Зафиксировать и отправить проверенную
версию в общий репозиторий в рамках командной процедуры. Для параллельной работы
напарнику нужна отправленная версия; локальные файлы автоматически не синхронизируются.
